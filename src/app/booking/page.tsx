import Booking from ".";

// Layout variant (same form and validation in both):
//   "one" — grey page, cover photo, one white card per section
//   "two" — white + primary: primary header band, shadowed cards, live summary
const Page = () => <Booking variant="one" />;

export default Page;
